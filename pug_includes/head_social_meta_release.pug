- let urlbase = 'http://badoptics.co';
- let image_meta = sharing_image || image;
//- Social tags
meta(
	name="twitter:title"
	content=`${title} - ${artist}`)/
meta(
	name="twitter:description"
	content=description_text)/
meta(
	name="twitter:image"
	content=`${urlbase}${image_meta.path}`)/
//- Twitter Summary card images must be at least 120x120px -->
meta(
	name="twitter:image"
	content=`${urlbase}${image.path}`)/
//-<meta name="twitter:creator" content="@author_handle">
//- Open Graph data
meta(
	name="og:title"
	content=`${title} - ${artist}`)/
meta(
	name="og:description"
	content=description_text)/
meta(
	name="og:url"
	content=`${urlbase}/${number}.html`)/
meta(
	name="og:image"
	content=`${urlbase}${image_meta.path}`)/
meta(
	name="og:image:width"
	content=image.width)/
meta(
	name="og:image:height"
	content=image.height)/